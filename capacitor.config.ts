import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.siddiquedev.nurulislam',
  appName: 'Nūr al-Islam',
  webDir: 'dist'
};

export default config;
